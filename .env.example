# Payment Architecture Configuration

# Paystack API Keys (Used for both NGN and USD transactions per request)
PAYSTACK_SECRET_KEY=
PAYSTACK_WEBHOOK_SECRET=

# Email Service Configuration
# For Gmail, use an App Password, not your normal Gmail password.
SMTP_URL=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=danielidah608@gmail.com
SMTP_PASS=

FIREBASE_SERVICE_ACCOUNT_KEY=

# Required in production; use the exact public origin serving the frontend.
FRONTEND_URL=
JWT_SECRET=
ALLOW_DEMO_TOKENS=false
